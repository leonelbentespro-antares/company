# ===================================================
# LexHub SaaS - Variáveis de Ambiente
# ===================================================
# Copie este arquivo para .env.local e preencha os valores
# NUNCA faça commit do .env.local no GitHub!
# ===================================================

# --------------------------------------------------
# SUPABASE (Frontend)
# --------------------------------------------------
VITE_SUPABASE_URL=https://SEU-PROJECT-ID.supabase.co
VITE_SUPABASE_ANON_KEY=sua_anon_key_do_supabase_aqui

# --------------------------------------------------
# SUPABASE (Backend / API Gateway)
# --------------------------------------------------
SUPABASE_URL=https://SEU-PROJECT-ID.supabase.co
SUPABASE_JWT_SECRET=seu_jwt_secret_do_supabase  # Settings > API > JWT Secret
SUPABASE_SERVICE_ROLE_KEY=sua_service_role_key   # Settings > API > service_role
SUPABASE_REF=SEU-PROJECT-ID                       # Para headers de segurança

# --------------------------------------------------
# REDIS (Message Broker / Filas)
# --------------------------------------------------
REDIS_URL=redis://:REDIS_PASSWORD@localhost:6379
REDIS_PASSWORD=gere_uma_senha_forte_aqui          # openssl rand -hex 32

# --------------------------------------------------
# META / WHATSAPP API
# --------------------------------------------------
META_VERIFY_TOKEN=token_de_verificacao_meta       # Você define este valor
META_APP_SECRET=app_secret_do_facebook_developers # Para validação HMAC

# --------------------------------------------------
# API GATEWAY — SEGURANÇA
# --------------------------------------------------
# Origins permitidas pelo CORS (separar por vírgula)
ALLOWED_ORIGINS=https://app.lexhub.com.br,https://lexhub.com.br

# Secret para rotas internas de admin (/internal/*)
# Nunca expor — usar apenas dentro da rede interna
INTERNAL_ADMIN_SECRET=gere_um_secret_forte_aqui  # openssl rand -hex 32

PORT=3001
NODE_ENV=production

# --------------------------------------------------
# IA
# --------------------------------------------------
GEMINI_API_KEY=sua_chave_gemini_aqui
OPENAI_API_KEY=sua_chave_openai_aqui              # Opcional

# --------------------------------------------------
# STRIPE (Pagamentos)
# --------------------------------------------------
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...
