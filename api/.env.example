# ============================================================
# LEXHUB CORE API — VARIÁVEIS DE AMBIENTE
# ============================================================
# Copie este arquivo para .env e preencha os valores reais.
# NUNCA faça commit do .env (já está no .gitignore)
# ============================================================

# --- SEGURANÇA ---
# JWT Secret do Supabase: Settings → API → JWT Secret
SUPABASE_JWT_SECRET=your_supabase_jwt_secret_here

# --- SUPABASE ---
# Settings → API → Project URL
SUPABASE_URL=https://SEU_PROJETO.supabase.co
# Settings → API → service_role key (CUIDADO: nunca expor no frontend!)
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# --- REDIS (Upstash) ---
# Upstash.com → Create Database → Redis → REST URL ou Connection String
# Formato: rediss://default:PASSWORD@HOST:PORT
REDIS_URL=rediss://default:YOUR_TOKEN@your-redis.upstash.io:6379

# --- CORS ---
# Origins permitidas pelo backend (sem espaços)
ALLOWED_ORIGINS=http://localhost:5173

# --- SERVIDOR ---
PORT=3001

# --- META / WHATSAPP API OFICIAL (opcional) ---
META_APP_SECRET=
META_VERIFY_TOKEN=
